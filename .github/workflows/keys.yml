name: Use the API key

on:
  push:
    branches: [ develop ]

jobs:
  use-api-key:
    runs-on: ubuntu-latest
    steps:
    - name: Use the API key
      run: echo $API_KEY
      env:
        API_KEY: ${{ secrets.API_KEY }}
